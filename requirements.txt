# Runtime (required by end users / Rhino 8 CPython)
# Installed into Rhino on first run via `# r:` directives in component code.py.
psycopg2>=2.9    # DB components (`# r: psycopg2-binary`)
shapely>=2.0     # geometry/WKT components (`# r: shapely`)
# SVG/chart components build SVG strings with pure stdlib — no svgwrite/matplotlib.

# Development (build, tests, decoding tools)
python-dotenv
pytest
pythonnet>=3.0
requests
